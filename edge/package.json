{
  "name": "@crankingai/edge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare edge Worker: watched-path logging with the DNT field-split (docs/edge.md).",
  "scripts": {
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir dist",
    "test": "vitest run",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9",
    "wrangler": "^4.107.0"
  }
}
